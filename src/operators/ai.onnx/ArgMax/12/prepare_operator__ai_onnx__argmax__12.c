//this file was generated by ../../../../../../scripts/onnx_generator/OperatorTemplate.py
#include "operator__ai_onnx__argmax__12.h"
#include "tracing.h"
#include "utils.h"

operator_status
prepare_operator__ai_onnx__argmax__12(
    node_context *ctx
)
{
    TRACE_ENTRY(1);

    TRACE_NODE(2, true, ctx->onnx_node);

    /* UNCOMMENT AS NEEDED */

    Onnx__TensorProto *i_data = searchInputByName(ctx, 0);

    TRACE_TENSOR(2, true, i_data);

    // Onnx__AttributeProto *a_axis = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"axis");
    // Onnx__AttributeProto *a_keepdims = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"keepdims");
    // Onnx__AttributeProto *a_select_last_index = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"select_last_index");

    // TRACE_ATTRIBUTE(2, a_axis, a_axis);
    // TRACE_ATTRIBUTE(2, a_keepdims, a_keepdims);
    // TRACE_ATTRIBUTE(2, a_select_last_index, a_select_last_index);

    Onnx__TensorProto *o_reduced = searchOutputByName(ctx, 0);

    /* ALLOCATE AND INITIALIZE CONTEXT HERE IF NEEDED */

    // int64_t default_axis = ;
    // int64_t default_keepdims = ;
    // int64_t default_select_last_index = ;

    // context_operator__ai_onnx__argmax__12 *op_ctx = NULL;
    // op_ctx = malloc(sizeof(context_operator__ai_onnx__argmax__12));
    // TRACE_FATAL(0 , !op_ctx, "could not allocate executer_context");

    // op_ctx->axis = a_axis?a_axis->i:default_axis;
    // op_ctx->keepdims = a_keepdims?a_keepdims->i:default_keepdims;
    // op_ctx->select_last_index = a_select_last_index?a_select_last_index->i:default_select_last_index;

    // TRACE_VAR(2, true, op_ctx->axis, "%" PRId64);
    // TRACE_VAR(2, true, op_ctx->keepdims, "%" PRId64);
    // TRACE_VAR(2, true, op_ctx->select_last_index, "%" PRId64);

    /* INITIALIZE OUTPUTS DATA_TYPE AND SHAPE HERE */

    // Allocate memory
    o_reduced->dims = malloc(i_data->dims[1] * sizeof(int64_t));

    // Populate some parameters
    o_reduced->n_dims       = 1;
    o_reduced->dims[0]      = i_data->dims[1];
    o_reduced->has_raw_data = 0;

    // INT64 is hardcoded by design
    o_reduced->data_type = ONNX__TENSOR_PROTO__DATA_TYPE__INT64;

    /* MALLOC OUTPUT TENSORS HERE */

    mallocTensorData(o_reduced);

    TRACE_TENSOR(2, true, o_reduced);

    /* CHOOSE EXECUTER AND CONTEXT HERE */
    /* YOU MAY USE THE GENERATED RESOLVER */

    ctx->executer = resolve_operator__ai_onnx__argmax__12(ctx);
    // ctx->executer_context = op_ctx;

    TRACE_EXIT(1);

    /* CHANGE RETURN CODE IF THIS PREPARER IS VALID */
    // return OP_ENOSYS;
    return OP_OK;
}